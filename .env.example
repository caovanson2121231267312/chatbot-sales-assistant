# Database Configuration (MySQL bên ngoài - host hoặc server khác)
# Chạy Docker: nếu MySQL trên máy host dùng host.docker.internal (Win/Mac)
# Linux: dùng IP máy host (vd 192.168.1.100) hoặc 172.17.0.1
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ecommerce_chatbot
DB_USER=root
DB_PASSWORD=

# Rasa Configuration
RASA_PORT=5005
ACTION_SERVER_PORT=5055
